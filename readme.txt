Funciones a implementar:
init() listo
mkdir() funcionando en relativo, no absoluto
ls() funcionando en relativo, no absoluto
mkf() funcionando en relativo, no absoluto
rename() funcionando en relativo, no absoluto
cat() funcionando en relativo, no absoluto (devuelve un string que hay que liberar) mirar checkBeforeCat
rmf() funcionando en relativo, no absoluto

rmdir() funcionando en relativo, no absoluto. Hay que hacer que borre a los hijos usando rmdir.
exit() a implementar
scp()/mv() a implementar.

Prueba